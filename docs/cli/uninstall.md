---
summary: "CLI reference for `nexa uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `nexa uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
nexa uninstall
nexa uninstall --all --yes
nexa uninstall --dry-run
```
